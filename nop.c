#include "monty.h"

/**
 * nop - this code does nothinf.
 * @stack: head
 * @num_line: number of the line
 */
void nop(stack_t **stack, unsigned int num_line)
{
	if (*stack)
		;
	if (num_line)
		;
}

