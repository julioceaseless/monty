#include "monty.h"
/**
 * stack - sets the format of the data to a stack (LIFO)
 * @stack: head
 * @num_line: num line
 */

void stack(stack_t **stack, unsigned int num_line)
{
	if (stack || num_line)
		global.mode = 1;
}

